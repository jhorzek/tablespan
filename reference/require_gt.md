# require_gt

Check that gt is installed

## Usage

``` r
require_gt(throw = TRUE)
```

## Arguments

- throw:

  throw error if the package is not installed

## Value

boolean or error

## Examples

``` r
library(tablespan)
require_gt()
#> [1] TRUE
```
